root = "."
tmp_dir = "tmp/api-gateway"
env_file = ".env"

[build]
cmd = "go build -o ./tmp/api-gateway/main ./services/api-gateway/cmd/server"
bin = "./tmp/api-gateway/main"
full_bin = "./tmp/api-gateway/main"

include_ext = ["go", "env", "proto"]
include_dir = ["services/api-gateway", "pkg", "proto"]

exclude_dir = ["tmp", "vendor", "node_modules", "db/data"]

delay = 1000
stop_on_error = true
send_interrupt = true
kill_delay = 500

[log]
time = true

[color]
main = "magenta"
watcher = "cyan"
build = "yellow"
runner = "green"

[misc]
clean_on_exit = true